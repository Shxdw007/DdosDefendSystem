<Window x:Class="DdosDefendSystem.AdminPanel.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Login" Height="480" Width="520"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource BorderBrushAccent}" BorderThickness="2" CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{DynamicResource TitleBarBrush}">
                <TextBlock Text="DDoS Defense — Authentication" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="12,0"/>
                <Button Content="✕" HorizontalAlignment="Right" Width="36" Background="Transparent"
                        BorderThickness="0" Foreground="{DynamicResource TextPrimaryBrush}"
                        FontWeight="Bold" Click="CloseButton_Click"/>
            </Grid>

            <StackPanel Grid.Row="1" Margin="40,30">
                <TextBlock Text="Network Security Monitor" FontSize="22" FontWeight="Bold"
                           Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Войдите для доступа к панели управления"
                           Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Username" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>

                <TextBlock Text="Password" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,20"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource ErrorBrush}"
                           Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"
                           HorizontalAlignment="Center" Margin="0,0,0,12"/>

                <Button Content="Войти" Style="{StaticResource PrimaryButton}" Height="40"
                        Command="{Binding LoginCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
