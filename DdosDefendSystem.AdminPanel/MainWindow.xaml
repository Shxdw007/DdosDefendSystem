<Window x:Class="DdosDefendSystem.AdminPanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Network Security Monitor" Height="650" Width="1100"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Loaded="MainWindow_Loaded" Unloaded="MainWindow_Unloaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource BorderBrushAccent}" BorderThickness="2" CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{DynamicResource TitleBarBrush}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                    <TextBlock Text="DDoS Defense Panel" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding CurrentUsername, StringFormat='  —  {0}'}"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="4,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding ThemeToggleLabel}" Width="36" ToolTip="Переключить тему"
                            Command="{Binding ToggleThemeCommand}" Background="Transparent" BorderThickness="0"
                            Foreground="{DynamicResource AccentBrush}" FontSize="16"/>
                    <Button Content="_" Width="36" Background="Transparent" BorderThickness="0"
                            Foreground="{DynamicResource TextPrimaryBrush}" Click="MinimizeButton_Click"/>
                    <Button Content="✕" Width="36" Background="Transparent" BorderThickness="0"
                            Foreground="{DynamicResource TextPrimaryBrush}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Traffic Scanner &amp; Firewall System"
                           FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,16"/>

                <TabControl Grid.Row="1" Background="Transparent" BorderBrush="{DynamicResource BorderBrushAccent}" BorderThickness="0,1,0,0"
                            ItemsSource="{Binding Tabs}">
                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Header}"/>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                    <TabControl.ContentTemplate>
                        <DataTemplate>
                            <ContentControl Content="{Binding Content}" Margin="0,8,0,0"/>
                        </DataTemplate>
                    </TabControl.ContentTemplate>
                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>
